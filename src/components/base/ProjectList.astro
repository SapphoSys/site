---
import Project from '$components/base/Project.astro';
import { projects } from '$data/projects';
---

<div class="flex flex-col gap-4">
  {projects.map((project) => <Project {...project} />)}
</div>
